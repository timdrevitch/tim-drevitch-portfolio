import styled from "styled-components"

export const SMContainer = styled.div`
  /* position: fixed; */
  margin-top: 1rem;
  width: 150px;
  background: transparent;
  bottom: 0;
`
export const SMListSection = styled.ul`
  display: inline-block;
  margin: 0 auto 0 -40px;
  text-align: center;
`
export const SMListItem = styled.li`
  list-style: none;
  display: inline-block;
`
export const SMButton = styled.button`
  background: none;
  border: none;
  cursor: pointer;
`
